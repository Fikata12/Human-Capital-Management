@using System.Security.Claims
@using HCM.Web.ViewModels.User

<ul class="navbar-nav ms-auto">
    @if (User.Identity?.IsAuthenticated ?? false)
    {
        <li class="nav-item px-1">
            <form class="d-inline" method="post" asp-controller="User" asp-action="Logout">
                <button type="submit" class="nav-link btn btn-link text-dark p-0 border-0" style="font-size: 1.2rem;">
                    <i class="bi bi-box-arrow-right"></i>
                    <span>Logout</span>
                </button>
            </form>
        </li>
    }
    else
    {
        <li class="nav-item px-1">
            <a class="nav-link text-dark" asp-controller="User" asp-action="Register">
                <i class="bi bi-person-plus" style="font-size: 1.2rem;"></i>
                <span>Register</span>
            </a>
        </li>
        <li class="nav-item px-1">
            <a class="nav-link text-dark" asp-controller="User" asp-action="Login">
                <i class="bi bi-box-arrow-in-right" style="font-size: 1.2rem;"></i>
                <span>Login</span>
            </a>
        </li>
    }
</ul>
